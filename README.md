# Query_Craft--Frontend
QueryCraft is an intelligent database assistant that converts natural language queries into executable SQL or NoSQL commands. It allows users to interact with databases using plain English instead of writing complex queries manually.  The system analyzes user intent, generates optimized database queries, and retrieves results in real time.
